/** Warm Cream, the page background (§15 --background). The PWA theme and background colour. */
export const PAGE_BACKGROUND = "#f9f8f6";

/** Asset URLs must carry the GitHub Pages base path (§3). */
export const BASE_PATH = process.env.NEXT_PUBLIC_BASE_PATH ?? "";
