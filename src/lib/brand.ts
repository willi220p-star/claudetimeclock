/** The page background (--background in globals.css). The PWA theme and background colour. */
export const PAGE_BACKGROUND = "#f2f2f7";

/** Asset URLs must carry the GitHub Pages base path (§3). */
export const BASE_PATH = process.env.NEXT_PUBLIC_BASE_PATH ?? "";
